# Fabric get_link Fact

The `Fabric` struct provides a `get_link(&self, a: &RouterId, b: &RouterId) -> Option<&Link>` helper that retrieves the link between two routers, simplifying multipath routing and link simulation.
