# Unused Import Cleanup Fact

The `src/tun/mod.rs` module no longer contains the commented-out unused import `// use std::time::Duration;`. Removing dead code/comments keeps the codebase tidy and prevents confusion about future usage.
