# IPv6 Packet Parsing Fact

The simulator now fully parses IPv6 packets in `src/packet/mod.rs`. It extracts source/destination IPs, next‑header, hop‑limit, and ports when applicable, enabling proper handling of IPv6 traffic throughout the simulation.
