# Remove Unused Imports in src/tun/mod.rs

**Summary**: The file `src/tun/mod.rs` contained unused imports (`std::net::Ipv4Addr` and `std::os::unix::io::FromRawFd`).

**Fix Summary**: Deleted the unused import lines. This eliminates compiler warnings and cleans up the code.

**Location**: Top of `src/tun/mod.rs`.

**Effort Estimate**: Small.
