problem: when running with real tun interfaces, the code exits on its own after a few seconds.

Expected behavior: when not running in mock mode, the code should keep running with real tun until interrupted.
